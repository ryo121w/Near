{
  "name": "my-entire-project",
  "version": "0.1.0",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "heroku-prebuild": "echo 'Skip this step or do some prebuild task'",
    "heroku-postbuild": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "devDependencies": {
  }
}
